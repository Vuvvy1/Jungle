<td>
  <tr>
    <%= image_tag product.image.tiny.url %>
  </tr>
  <tr>
    <h4><%= product.name %></h4>
    <br>
    <p><%= product.description %></p>
  </tr>
  <tr>
    <!-- <%= product.quantity %> -->
  </tr>
  <tr>
    <%= humanized_money_with_symbol product.price_cents * item.quantity / 100.00 %>
  </tr>
</td>
